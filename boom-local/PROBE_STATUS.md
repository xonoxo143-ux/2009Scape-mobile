# Boom localhost probe

This marker triggers the first disposable GitHub Actions run against the localhost client/server probe.

Goal: capture the client's initial revision-239 config/JS5/login traffic without contacting a gameplay server after endpoint rewriting.
